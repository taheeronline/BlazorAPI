@page "/books"
@rendermode InteractiveServer
@inject BlazorAPI.BlazorUI.Services.Interface.IBookService BookService
@inject NavigationManager NavigationManager
@inject IJSRuntime JS

<PageTitle>Book List</PageTitle>

<div class="container-fluid mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-book-half"></i> Book Library</h2>
        <AuthorizeView>
            <Authorized>
                <button class="btn btn-primary shadow-sm" @onclick="AddBook">
                    + Add Book
                </button>
            </Authorized>
        </AuthorizeView>
    </div>

    <div class="card shadow-sm border-0 mb-4 bg-light">
        <div class="card-body py-3">
            <div class="row gx-2">
                <div class="col-md-3">
                    <select class="form-select" @bind="searchType">
                        <option value="Title">Search by Title</option>
                        <option value="Author">Search by Author</option>
                        <option value="Publisher">Search by Publisher</option>
                    </select>
                </div>
                <div class="col-md-6">
                    <input type="text" class="form-control" placeholder="Enter search term..." @bind="searchTerm" @bind:event="oninput" @onkeyup="HandleKeyUp" />
                </div>
                <div class="col-md-3 d-flex gap-2">
                    <button class="btn btn-secondary w-100" @onclick="ExecuteSearch">
                        <i class="bi bi-search"></i> Search
                    </button>
                    <button class="btn btn-outline-secondary w-100" @onclick="ClearSearch">
                        Clear
                    </button>
                </div>
            </div>
        </div>
    </div>

    @if (isLoading)
    {
        <div class="d-flex justify-content-center my-5">
            <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">Loading...</span>
            </div>
        </div>
    }
    else if (errorMessage != null)
    {
        <div class="alert alert-danger shadow-sm">
            @errorMessage
        </div>
    }
    else if (pagedResult == null || !pagedResult.Items.Any())
    {
        <div class="alert alert-info shadow-sm">
            No books found matching your criteria.
        </div>
    }
    else
    {
        <div class="card shadow-sm border-0">
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered mb-0">
                        <thead class="table-dark">
                            <tr>
                                <th style="width: 25%;">Title</th>
                                <th style="width: 30%;">Description</th>
                                <th style="width: 15%;">Author</th>
                                <th style="width: 10%;">Publisher</th>
                                <th style="width: 8%;">Price</th>
                                <AuthorizeView>
                                    <Authorized>
                                        <th class="text-center" style="width: 12%;">Actions</th>
                                    </Authorized>
                                </AuthorizeView>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var book in pagedResult.Items)
                            {
                                <tr class="align-middle">
                                    <td class="fw-bold text-primary">@book.Title</td>
                                    <td>
                                        <span class="text-truncate d-inline-block" style="max-width: 250px;" title="@book.Description">
                                            @book.Description
                                        </span>
                                    </td>
                                    <td>@book.Author</td>
                                    <td>@book.Publisher</td>
                                    <td>$@book.Price.ToString("0.00")</td>
                                    <AuthorizeView>
                                        <Authorized>
                                            <td class="text-center">
                                                <div class="btn-group" role="group">
                                                    <button class="btn btn-sm btn-outline-primary" @onclick="() => EditBook(book)">
                                                        Edit
                                                    </button>
                                                    <button class="btn btn-sm btn-outline-danger" @onclick="() => DeleteBook(book)">
                                                        Delete
                                                    </button>
                                                </div>
                                            </td>
                                        </Authorized>
                                    </AuthorizeView>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="card-footer bg-white d-flex justify-content-between align-items-center py-3">
                <span class="text-muted fw-bold">
                    Page @pagedResult.CurrentPage of @pagedResult.TotalPages
                    <span class="fw-normal ms-2">(Total: @pagedResult.TotalCount records)</span>
                </span>
                <div class="btn-group shadow-sm">
                    <button class="btn btn-outline-primary" disabled="@(!pagedResult.HasPrevious)" @onclick="GoToPreviousPage">
                        Previous
                    </button>
                    <button class="btn btn-outline-primary" disabled="@(!pagedResult.HasNext)" @onclick="GoToNextPage">
                        Next
                    </button>
                </div>
            </div>
        </div>
    }
</div>
<br />
<br />

@code {
    [CascadingParameter]
    private Task<AuthenticationState>? AuthStateTask { get; set; }

    private PagedResult<BookDTO>? pagedResult;
    private string searchType = "Title";
    private string searchTerm = string.Empty;
    private bool isLoading = true;
    private string? errorMessage;

    // Pagination State
    private int currentPage = 1;
    private int pageSize = 5;

    protected override async Task OnInitializedAsync()
    {
        await FetchData();
    }

    private async Task ExecuteSearch()
    {
        currentPage = 1;
        await FetchData();
    }

    private async Task FetchData()
    {
        isLoading = true;
        errorMessage = null;

        try
        {
            if (string.IsNullOrWhiteSpace(searchTerm))
            {
                pagedResult = await BookService.GetAllAsync(currentPage, pageSize);
            }
            else
            {
                switch (searchType)
                {
                    case "Author":
                        pagedResult = await BookService.GetByAuthorAsync(searchTerm, currentPage, pageSize);
                        break;
                    case "Publisher":
                        pagedResult = await BookService.GetByPublisherAsync(searchTerm, currentPage, pageSize);
                        break;
                    case "Title":
                    default:
                        pagedResult = await BookService.GetByTitleAsync(searchTerm, currentPage, pageSize);
                        break;
                }
            }
        }
        catch (HttpRequestException ex)
        {
            Console.WriteLine($"Network or API error: {ex.Message}");
            errorMessage = "Failed to communicate with the server. Please try again.";
            pagedResult = new PagedResult<BookDTO> { Items = Enumerable.Empty<BookDTO>() };
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Unexpected error: {ex.Message}");
            errorMessage = "An unexpected error occurred while loading data.";
            pagedResult = new PagedResult<BookDTO> { Items = Enumerable.Empty<BookDTO>() };
        }
        finally
        {
            isLoading = false;
        }
    }

    private async Task ClearSearch()
    {
        searchTerm = string.Empty;
        currentPage = 1;
        await FetchData();
    }

    private async Task HandleKeyUp(KeyboardEventArgs e)
    {
        if (e.Key == "Enter")
        {
            await ExecuteSearch();
        }
    }

    // --- Pagination Navigation ---

    private async Task GoToPreviousPage()
    {
        if (pagedResult?.HasPrevious == true)
        {
            currentPage--;
            await FetchData();
        }
    }

    private async Task GoToNextPage()
    {
        if (pagedResult?.HasNext == true)
        {
            currentPage++;
            await FetchData();
        }
    }

    // --- Actions ---

    private void AddBook()
    {
        NavigationManager.NavigateTo("/books/add");
    }

    private void EditBook(BookDTO book)
    {
        NavigationManager.NavigateTo($"/books/edit/{book.Id}");
    }

    private async Task DeleteBook(BookDTO book)
    {
        bool confirmed = await JS.InvokeAsync<bool>("confirm", $"Are you absolutely sure you want to delete '{book.Title}'?");

        if (confirmed)
        {
            try
            {
                int modifiedById = 0;

                if (AuthStateTask != null)
                {
                    var authState = await AuthStateTask;
                    var user = authState.User;

                    if (user.Identity is not null && user.Identity.IsAuthenticated)
                    {
                        var userIdString = user.FindFirst(ClaimTypes.NameIdentifier)?.Value;
                        int.TryParse(userIdString, out modifiedById);
                    }
                }

                await BookService.DeleteBookAsync(book.Id, modifiedById);

                // Reload the current page to reflect the deletion
                await FetchData();
            }
            catch (HttpRequestException ex)
            {
                Console.WriteLine($"API Error deleting book: {ex.Message}");
                errorMessage = "Failed to delete the book. Ensure you have the right permissions.";
            }
            catch (Exception ex)
            {
                Console.WriteLine($"Error deleting book: {ex.Message}");
                errorMessage = "An unexpected error occurred during deletion.";
            }
        }
    }
}